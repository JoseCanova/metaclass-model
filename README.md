# metaclass-model
common model for schema-metaclass-project
#### next steps for this project is to clean jpa annotations and generate the json schema from the metamclass model.
https://json-schema.org/understanding-json-schema/about
##### the main objective to generate the json schema is to maintain the data structure documented and "programming language" independent providing the vocabulary to generate "in near future" an editor of the metaclass model. 
